import React, {useState} from 'react';

interface TestProps {
    
}

const Test = () => {
    return (
        <div>
            <button
                // onClick={() => {console.log(testApi())}}
            >click!</button>
        </div>
    );
}

export default Test;