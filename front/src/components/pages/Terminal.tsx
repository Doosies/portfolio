
import styled from 'styled-components';

const TerminalBlock = styled.div`
    
`
const Terminal = () => {
    return (
        <TerminalBlock>
            t
        </TerminalBlock>
    );
}

export default Terminal;