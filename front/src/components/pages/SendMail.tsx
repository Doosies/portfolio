import styled from 'styled-components';

const SendMailBlock = styled.div`
    
`
const SendMail = () => {
    return (
        <SendMailBlock>
            s
        </SendMailBlock>
    );
}

export default SendMail;